<div class="row">
    <h1>@{{ metal.name }}</h1>

    <div ng-show="metal.name == 'Aluminium'">
        this is info about aluminium
    </div>

    <div ng-show="metal.name == 'Brass'">
        this is info about brass
    </div>

</div>
